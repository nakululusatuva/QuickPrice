# Caddy receives only these non-provider settings. Do not copy the QuickPrice
# application environment into the Caddy service.
QUICKPRICE_DOMAIN=price.example.com
ACME_EMAIL=you@example.com
QUICKPRICE_CADDY_ACCESS_LOG_OUTPUT=stdout
